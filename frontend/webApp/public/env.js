window.APP_ENV = {
  SERVER_HOST: "${SERVER_HOST}",
  SERVER_PORT: "${SERVER_PORT}",
  APP_IS_HTTPS: "${APP_IS_HTTPS}",
  APP_DEBUGGABLE: "${APP_DEBUGGABLE}",
  APP_VAPID_PUBLIC_KEY: "${APP_VAPID_PUBLIC_KEY}"
};