import utils.types.BigDecimal;

CREATE TABLE IF NOT EXISTS WeatherEntity (
    temperature TEXT AS BigDecimal NOT NULL,
    unit TEXT NOT NULL
);

deleteAll:
DELETE FROM WeatherEntity;

insertWeather:
INSERT OR REPLACE INTO WeatherEntity(temperature, unit) VALUES (?, ?);

selectAll:
SELECT * FROM WeatherEntity;